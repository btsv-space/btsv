run:
  timeout: 2m

linters:
  enable:
    - errcheck
    - gosec
    - govet
    - staticcheck
    - unused
    - gofmt
    - goimports

linters-settings:
  goimports:
    local-prefixes: github.com/btsv/btsv/api

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
